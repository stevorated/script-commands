# Context-Scripts 

## Main Functionalities

Scripts to add and remove an IDE to context menu

### Use

- git clone this repo `git clone git@github.com:stevorated/script-commands.git context-scripts`
- update `LaunchPath` & `IconPath` & `AppName` values 
- open bash as admin
- run `addContext` to add
or
- run `addContext` to remove
- You Are Good To Go 🚀 💣